[package]
name = "shared"
version = "0.1.0"
edition = "2024"

[dependencies]
config = { path = "../config" }

axum = "0.8.9"
sqlx = { version = "0.9.0", default-features = false, features = [
    "runtime-tokio",
    "tls-rustls",
    "postgres",
] }