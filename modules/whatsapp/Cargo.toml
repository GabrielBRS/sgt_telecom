[package]
name = "whatsapp"
version = "0.1.0"
edition = "2024"

[dependencies]
domain = { path = "../../domain" }
config = { path = "../../config" }
shared = { path = "../../shared" }
compute = { path = "../../compute" }
